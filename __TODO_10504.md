TODO:

- Remove pk/sk from getCaseByDocketNumber (in both 10491 and 10504)
- numberOfPages commented out in upsertCaseCorrespondence??
- case-worksheets| -- find and remove
- use of getFreshMockCase -- before, we sent undefined, so the caseStatusHistory was always fresh; now, following the type, we send in [], so that it is updated (Case.caseStatusHistory is assigned to the reference of rawCase.caseStatusHistory). Should we pass in undefined instead and change the type rule?