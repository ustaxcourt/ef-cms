# TO DO
- Do a search of all user|, section|, irsPractitioner|, etc and purge it
- Opensearch sync + migration dynamo->postgres
  - {pk: user|, sk: user|}
  - {pk: case|, sk: irsPractitioner|}
  - {pk: case|, sk: privatePractitioner|}
  - {pk: user|, sk: case|}
- Manual Postgres migration from dwCase.petitioners -> dwUser/dwUserOnCase


# Manual Deployment
- Turn on maintenance mode
- move petitioners from dwCase.petitioners -> dwUser via script
  - run environment switcher
  - run script: `npx ts-node --transpile-only scripts/run-once-scripts/postgres-migration/move-petitioners-out-of-case.ts`
- Move users, privatePractitioners, irsPractitioners, pending-case from dynamo to postgres via script 
  - run environment switcher
  - run script: `npx ts-node --transpile-only scripts/run-once-scripts/postgres-migration/migrate-users-to-postgres.ts`
- Deploy + switch color
- Turn off maintenance mode

## Problems with petitioners on separate table
- Order in the petitioners array is important
- Potential performance issue with fetching petitioners
- Because we upsert cases when we edit a petitioner we can potentially screw up a petitioner by deleting them, or editing them incorrectly. 



How do we handle the opensearch sync situation when we are upserting user onCase?
- When we upsert user on case we send a message to a lambda
- The lambda then needs to refetch the whole case and save it to opensearch
- When it tries to fetch the case it will not have all of the data as we have not finished moving it over to postgres.

Can we just not update opensearch when we run the migration scripts?
- We are just moving data from dynamo -> postgres and dwCase.petitioners -> dwUser not actually modifying it. At least not intentionally.
- If opensearch was turned off we could just write all the data to postgres, then deploy, and then any updates will send messages to opensearch correctly.
- How can we make sure that we have not screwed up