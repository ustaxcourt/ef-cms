# TO DO
- Run scripts/run-once-scripts/postgres-migration/delete-users.ts on test
- test speed of web-api/src/persistence/postgres/users/createBarNumber.ts function and understand the sql
- test speed of getPractitionersBySearchKey function


# Manual Deployment
- NOTE: This deployment CANNOT have a regular migration with it.
- Turn on maintenance mode
  - run environment switcher
  - run command: `npm run maintenance:engage`
- migrate the postgres db forward.
  - run environment switcher
  - run command: `npm run migration:postgres`
- move petitioners from dwCase.petitioners -> dwUser via script
  - run environment switcher
  - run script: `npx ts-node --transpile-only scripts/run-once-scripts/postgres-migration/move-petitioners-out-of-case.ts`
- Move users, privatePractitioners, irsPractitioners, pending-case from dynamo to postgres via script 
  - run environment switcher
  - run script: `npx ts-node --transpile-only scripts/run-once-scripts/postgres-migration/migrate-users-to-postgres.ts`
- Deploy + switch color
- Turn off maintenance mode
  - run environment switcher
  - run command: `npm run maintenance:disengage`
- Run readonly smoketests against prod to test deployment worked
  - run environment switcher
  - enable smoketest user:  `./scripts/user/create-and-enable-smoketest-user.ts`
  - run readonly smoketests: `./scripts/run-cypress.sh -rc`
  - disable smoketests: `./scripts/user/disable-smoketest-user.ts`
  - run public readonly smoketests: `./scripts/run-cypress.sh -rpc`
- AT SOME POINT LATER: Delete all user data from dynamo
  - run environment switcher
  - run script: `npx ts-node --transpile-only scripts/run-once-scripts/postgres-migration/delete-users.ts` 

## Problems with petitioners on separate table
- Order in the petitioners array is important
- Potential performance issue with fetching petitioners
- Because we upsert cases when we edit a petitioner we can potentially screw up a petitioner by deleting them, or editing them incorrectly. 
