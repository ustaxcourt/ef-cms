# To Do
- Remove all extraneous fields from workItem
  - Remove all places that we do unecessary upsertWorkItem() as we no longer need to modify workItem when things on the case change
  - Find places that you need associatedJudge, caseStats and merge WorkItems with Case Entity data wise
- Remove docketEntry from WorkItem (spiderman problem).
- When creating work items in a consolidated group make multiple work items or have a work item contain multiple cases.

## Bonus To Do
- Remove dataSecurityFilter function

## Follow up clean up story
- Remove columns from workItem table that are not needed any more

## Misc.
In non-technical terms, describe and model the lifecycle of a "Work Item"
- What is the purpose of a Work Item?
- When are Work Items created, and by whom?
- What happens to a Work Item after it is created?
- Does a Work Item's lifecycle "end" in the current design? Should it?

```typescript
// via `git show 16f12f6f7ca467fa9f98a2a7d3393ce5dd2481aa~1:web-api/src/persistence/elasticsearch/workitems/getDocumentQCInboxForSection.ts`
import { GET_PARENT_CASE } from '../helpers/searchClauses';
import { search } from '../searchClient';

export const getDocumentQCInboxForSection = async ({
  applicationContext,
  judgeUserName,
  section,
}) => {
  applicationContext.logger.info('getDocumentQCInboxForSection start');
  const query = {
    body: {
      query: {
        bool: {
          must: [
            {
              prefix: { 'pk.S': 'case|' },
            },
            {
              prefix: { 'sk.S': 'work-item|' },
            },
            {
              term: {
                'section.S': section,
              },
            },
          ],
          must_not: {
            exists: {
              field: 'completedAt.S',
            },
          },
          should: [
            {
              term: {
                'highPriority.BOOL': {
                  boost: 500,
                  value: true,
                },
              },
            },
            GET_PARENT_CASE,
          ],
        },
      },
      size: 5000,
    },
    index: 'efcms-work-item',
  };

  if (judgeUserName) {
    query.body.query.bool.must.push({
      match: {
        'associatedJudge.S': `${judgeUserName}`,
      },
    });
  }

  const { results } = await search({
    applicationContext,
    searchParameters: query,
  });

  applicationContext.logger.info('getDocumentQCInboxForSection end');

  return results;
};
```