# To Do
- Remove all extraneous fields from workItem
  - Remove all places that we do unecessary upsertWorkItem() as we no longer need to modify workItem when things on the case change
  - Find places that you need associatedJudge, caseStats and merge WorkItems with Case Entity data wise
- Remove docketEntry from WorkItem (spiderman problem).
- When creating work items in a consolidated group make multiple work items or have a work item contain multiple cases.

## Bonus To Do
- Remove dataSecurityFilter function

## Follow up clean up story
- Remove columns from workItem table that are not needed any more

## Misc.
In non-technical terms, describe and model the lifecycle of a "Work Item"
- What is the purpose of a Work Item?
- When are Work Items created, and by whom?
- What happens to a Work Item after it is created?
- Does a Work Item's lifecycle "end" in the current design? Should it?

### WorkItem using docketEntry(zach investigation)
web-client/src/views/WorkQueue/IndividualWorkQueueInbox.tsx
- workItem.docketEntry.documentType
- workItem.docketEntry.filedBy

web-client/src/presenter/computeds/formattedWorkQueue.ts
- workItem.docketEntry.additionalInfo
- workItem.docketEntry.createdAt
- workItem.docketEntry.documentTitle
- workItem.docketEntry.documentType
- workItem.docketEntry.eventCode
- workItem.docketEntry.receivedAt
- workItem.docketEntry.servedAt
- formattedDocketEntry
  - formattedDOcketEntry.IsInProgress
  - formattedDOcketEntry.IsPetition
  - formattedDOcketEntry.isPaper
  - formattedDOcketEntry.isCourtIssuedDocument

WorkItem.ts
  'additionalInfo',
  'createdAt',
  'descriptionDisplay',
  'docketEntryId',
  'documentTitle',
  'documentType',
  'eventCode',
  'filedBy',
  'index',
  'isFileAttached',
  'isPaper',
  'otherFilingParty',
  'receivedAt',
  'sentBy',
  'servedAt',
  'userId',

Thoughts
- It seems like we almost never use WorkItem on the backend. We do not read any data from a workItem.
- Work items seem to only be used on the frontend in the documentQC panel.
- If we do not use the data for workItems on the backend when performing business work then it should be okay to separate the docketEntry from the entity

### DocketEntry using workItem

Thoughts
- In the long long ago there was no such thing as a workItem, it just existed on a docketEntry. Eventually a new Entity was created and we started storing separately
- Because we started storing them separately there are a lot of places where we are just updating a docketEntry but not actually reading anything from docketEntry.workItem in the backend.
   - web-api/src/business/useCaseHelper/docketEntry/fileAndServeDocumentOnOneCase.ts 
   - web-api/src/business/useCases/saveCaseDetailInternalEditInteractor.ts
- 