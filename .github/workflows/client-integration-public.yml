name: Node.js Public Client Integration Tests

on:
  pull_request:
    paths-ignore:
      - '**.md'
      - '**/.gitignore'
      - '.nvmrc'
      - '.circleci/config.yml'
      - 'docs/**'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref || github.run_id }}
  cancel-in-progress: true

permissions:
  actions: read
  contents: read

jobs:
  client_integration_public:
    strategy:
      fail-fast: false
      matrix:
        ci_node_total: [2]
        ci_node_index: [0, 1]
    uses: ./.github/workflows/template_app-parallel.yml
    with:
      ci_node_index: ${{ matrix.ci_node_index }}
      ci_node_total: ${{ matrix.ci_node_total }}
      configure_sysctl: true
      extra_artifact_name: server-log-output-${{ matrix.ci_node_index }}
      extra_artifact_path: /tmp/web-client/server-output${{ matrix.ci_node_index }}.txt
      fetch_depth: 0
      record_jest_test_timings: true
      run_command: |
        npm run migration:postgres
        npm run migration:postgres:contract
        npm run seed:postgres
        mkdir -p /tmp/test-file-times
        mkdir -p /tmp/web-client
        npm run start:api:ci > /tmp/web-client/server-output${{ matrix.ci_node_index }}.txt &
        ./wait-until-services.sh
        export TESTFILES=$(scripts/github-actions/split-tests.ts -public)
        export JEST_TEST_RESULTS_ARGS="--json --outputFile=${JEST_TEST_RESULTS_FILE}"
        npm run test:client:integration:ci
      workflow_filename: client-integration-public.yml
  persist-test-file-times:
    needs: [client_integration_public]
    uses: ./.github/workflows/job_persist-test-file-times.yml
    with:
      workflow_filename: client-integration-public.yml
