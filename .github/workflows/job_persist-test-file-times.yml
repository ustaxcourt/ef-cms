name: Persist Test File Times

on:
  workflow_call:
    inputs:
      workflow_filename:
        required: true
        type: string

permissions:
  actions: read
  contents: read

jobs:
  persist-test-file-times:
    runs-on: ubuntu-latest
    env:
      MERGED_TEST_FILE_TIMINGS_PATH: /tmp/test-file-times/${{ inputs.workflow_filename }}-historical-test-file-times.json
      SHARD_TEST_FILE_TIMINGS_DIR: /tmp/test-file-times/shards
    steps:
      - name: Checkout Current Branch
        uses: actions/checkout@v6
      - name: NPM Install in Current Branch
        uses: ./.github/actions/dawson-node-bootstrap
      - name: Download historical test file times
        uses: actions/download-artifact@v7
        with:
          pattern: ${{ inputs.workflow_filename }}-historical-test-file-times-shard-*
          merge-multiple: true
          path: ${{ env.SHARD_TEST_FILE_TIMINGS_DIR }}
      - name: Merge test file timings
        run: scripts/github-actions/test-file-times.ts merge-directory "$MERGED_TEST_FILE_TIMINGS_PATH" "$SHARD_TEST_FILE_TIMINGS_DIR"
      - name: Upload new historical test file times
        uses: actions/upload-artifact@v7
        with:
          name: ${{ inputs.workflow_filename }}-historical-test-file-times-${{ github.event.pull_request.head.sha || github.sha }}
          path: ${{ env.MERGED_TEST_FILE_TIMINGS_PATH }}
