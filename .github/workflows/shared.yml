name: Node.js CI - shared

on:
  pull_request:
  push:
    branches:
      - dependency-updates

jobs:
  Shared:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Shared
        run: |
          npm ci
          npm run test:shared
