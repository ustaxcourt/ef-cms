name: Node.js CI - Shared

on:
  pull_request:
    types: [opened, reopened, synchronize, ready_for_review]
    paths-ignore:
      - '**.md'
      - '**/.gitignore'
      - '.nvmrc'
      - '.circleci/config.yml'
      - 'docs/**'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref || github.run_id }}
  cancel-in-progress: true

permissions:
  actions: read
  contents: read
  pull-requests: write

jobs:
  Shared:
    if: ${{ !github.event.pull_request.draft }}
    strategy:
      fail-fast: false
      matrix:
        ci_node_total: [2]
        ci_node_index: [0, 1]
    uses: ./.github/workflows/template_no-app-parallel.yml
    with:
      ci_node_index: ${{ matrix.ci_node_index }}
      ci_node_total: ${{ matrix.ci_node_total }}
      coverage_source_dir: shared/coverage/${{ matrix.ci_node_index }}
      fetch_depth: 0
      splitter_command: scripts/github-actions/split-tests-glob.ts shared
      test_command: NODE_INDEX=${{ matrix.ci_node_index }} npm run test:shared:ci
      workflow_filename: shared.yml
  check-coverage:
    needs: [Shared]
    uses: ./.github/workflows/job_check-coverage.yml
    with:
      branches: 95
      coverage_suite: shared
      functions: 95
      lines: 95
      pull_request_number: ${{ github.event.pull_request.number }}
      statements: 95
      workflow_filename: shared.yml
  persist-test-file-times:
    needs: [Shared]
    uses: ./.github/workflows/job_persist-test-file-times.yml
    with:
      workflow_filename: shared.yml
  update-coverage-table:
    if: ${{ github.event.pull_request.base.ref == 'prod' }}
    needs: [check-coverage]
    runs-on: ubuntu-latest
    env:
      COMPOSITE_COVERAGE_SUMMARY_PATH: /tmp/shared-pr-${{ github.event.pull_request.number }}-coverage-summary.json
    steps:
      - name: Checkout Current Branch
        uses: actions/checkout@v6
      - name: NPM Install in Current Branch
        uses: ./.github/actions/dawson-node-bootstrap
      - name: Download composite coverage summary
        uses: actions/download-artifact@v7
        with:
          name: coverage-summary-shared-pr-${{ github.event.pull_request.number }}
          path: /tmp
      - name: Update coverage table
        run: |
          scripts/github-actions/update-prod-release-coverage.ts \
            "${COMPOSITE_COVERAGE_SUMMARY_PATH}" \
            ${{ github.event.pull_request.number }}
