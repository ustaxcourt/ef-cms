# TODO
- Talk about getting docket entries into postgres without using a migration
  - Keep streams so that writes during deploy are handled, but also have a script for oopsies so we don't always have to run a migration
  - file is migrate-all-docket-entries; this has not been tested
- spot check performance of getDocketEntriesByDocketNumberAndDocketEntryId running against test db
- withValidation is causing validation errors that did not previously exist (e.g., in createCaseInteractor!). How do we want to handle that?
- create script to delete all docket entry in dynamo
- clean up DocketEntry fields by comparing with full data from database (e.g., get rid of unused fields)