# TODO
- Talk about getting docket entries into postgres without using a migration
  - Keep streams so that writes during deploy are handled, but also have a script for oopsies so we don't always have to run a migration
  - file is migrate-all-docket-entries; this has not been tested
- test script to delete all docket entry in dynamo(scripts/run-once-scripts/postgres-migration/delete-all-docket-entries.ts)
- Figure out what to do with .signedAt
  - There are many docketEntries with signedAt set to 'Not in Blackstone' which means the column cannot be a timestamp.
  - these fields cannot be nulled out as we would then create invalid docketEntries as we require a signature.
