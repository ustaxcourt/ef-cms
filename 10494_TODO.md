Translation Playbook
- Get basic schema, either from script/entity (migration/types/mapper)
- Translate seed data (and type it)
- Go through Dynamo persistence methods --> make postgres versions
- Replace them
- Fix lots of things, spot checks
- Get data that is in dynamo into postgres
- Opensearch (send message with docketEntryId pre-queue, handle message, get docketEntry, insert to Opensearch)
- Fixing a ton of tests, mocks
- create script to delete all docket entry in dynamo

# TODO
- Check that cypress env stuff for postgres works in a deployed environment (it does work locally) (used by cypress/helpers/cypressTasks/wait-for-noce.ts)
- Talk about getting docket entries into postgres without using a migration
- spot check performance of getDocketEntriesByDocketNumberAndDocketEntryId running against test db
- Investigate connection pooling in database.ts (it looks like we are just creating a pool per-lambda invocation, not a cross-lambda pool)
  - The connection pooling will work on warm lambdas, but a more robust solution is an RDS proxy
- withValidation is causing validation errors that did not previously exist (e.g., in createCaseInteractor!). How do we want to handle that?
- Some things, like documentType, really seem like they shouldn't be optional. Should we investigate and manually set documentType/maybe other fields on docket entries that do not have them? This will determine how we handle type errors like the one in formatePendingItem.ts.