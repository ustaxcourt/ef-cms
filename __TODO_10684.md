Where is workItem used from getCaseByDocketNumber?
getFormattedCaseDetail
- we need completedAt to determine qcNeeded

docketEntryQcHelper
- we need completedAt and isRead to determine untouched

initForUnfilteredForInternalUsers on DocketEntry?? yuck

setQCWorkItemIdToMarkAsReadIfNeededAction

- we need workItemId and isRead

fileAndServeDocumentOnOneCase -- we attach work item ... but why? seems unused
completeDocketEntryQCInteractor -- same

saveCaseDetailInternalEdit -- we use work item here?

Goal: remove getWorkItems from getCaseByDocketNumber

- get case interactor can associate just the necessary data (isRead, completedAt, workItemId)
- endpoints can fetch workitems as needed


work item does not have docket entry, it has docket entry id
