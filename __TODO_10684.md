## TODO:
### Goals:
- Fix the too-much-data-being-returned error
- Fix the spiderman problem: docket entries have work items on them, and work items have docket entries on them
  - This means we need to keep the two of them in sync, which is fragile
  - It also means we sort of lie about types (docket entries do not have work items ... except when they do)
  - It also contributes to the too-much-data-being-returned error (based on some rough analysis of 16017-21, we should save ~.5 MB)
    - Obviously this is a bandaid! But it is a refactor that should happen anyway.
- See if there are other unused things that we might be able to filter.
  - The difficulty is that we use getCaseByDocketNumber in a lot of places, so it is hard to tease apart exactly what subset of data is needed when.

### Approach:
- Remove docket entries from work items and work items from docket entries. Join when needed.
- Return only what the UI needs for work items

## What the UI needs:
- I think just isRead, completedAt, and workItemId?

- getFormattedCaseDetail
  - we need completedAt to determine qcNeeded

- docketEntryQcHelper
  - we need completedAt and isRead to determine untouched

- setQCWorkItemIdToMarkAsReadIfNeededAction
  - we need workItemId and isRead

# TODO:
- Fix formattedWorkQueue (message never unbolded)
- hasWorkItemInfo is a hack. Can we improve it, at least with some sort of reusable function?
- same for attachWorkItemInfoForUI -- the lesser of two evils, but maybe there is a third option
- go through the api code and update so that we are not using docketEntry.workItem, etc.
- The one-time move to disassociate the work items and docket entries could entail data loss if we are not careful. (Maybe there was something saved on the nested docket entry in work item that was not reflected on the actual docket entry row.) Is this a problem, or can we treat the docket entry as the source of truth right from the get-go? If it is a problem, we need to determine if there are any disparities between work items' docket entries and the actual docket entries and find ways to fix them.

# Notes:
fileAndServeDocumentOnOneCase -- we attach work item to docket entry ... but why? seems unused
completeDocketEntryQCInteractor -- same

saveCaseDetailInternalEdit -- we use work item here?
