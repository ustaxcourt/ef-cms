Continue moving seed data:
• Cut a case from efcms-local.json (search for "sk": "case|", ignoring the first 23, which are mapping records) and paste in removed-from-efcms.json. This is mostly for record-keeping. We can delete this file when we are done.
• Paste into cases (cases.ts), in order by docket number. Add the docket number itself to SEEDED_DOCKET_NUMBERS.
• Cut the petitioners out from the case. Paste them into petitionerToCaseMappings. Also paste them into petitionerUsers (unless it is a duplicate).
• Remove the unnecessary fields in petitionerUsers. Remove the unnecessary fields in petitionerToCaseMappings, and add the docket number from SEEDED_DOCKET_NUMBERS.
• Fix the entry in cases, using constant when possible (see examples)
• If files get too big, break apart in a reasonable way

Figure out which fields we can get rid of

How do we handle mailingDate? It looks like it can maybe remain a string.

Refactor to use a case factory. See https://github.com/ustaxcourt/ef-cms/pull/5557/files for one idea.

In getDocumentQCInboxForUser, I had to add to falsy checks that can probably be removed once all the seed data is there.

Get sealCaseInteractor to work -- timezone issue

Associate the right things with a case efficiently in getCaseByDocketNumber
-- also, maybe we should pass flags (or something like that, but better) to determine which subsets of data we need
-- I doubt every call to getCaseByDocketNumber actually needs all of the docketEntries, petitioners, etc.