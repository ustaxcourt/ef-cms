Outstanding issues:

- Possible failure mode with interrupted Promise.all: an error leads to interrupted in-flight promises with the consequence of data in a corrupt state. We think this happened in setTrialSessionCalendarInteractor.ts.
  - transactions? Tricky to implement currently. The best way would probably be to wrap the entire thing (all of calendaring) in a transaction, but with some stuff in Dynamo, this is not quite ready. We could also instantiate a separate db connection in updateCase and wrap all those db calls in a transaction, but that would entail either opening a ton of connections or else cleverly managing connections.
    - I think we could do this. But it would be tricky.
  - we could go through our usages of Promise.all in ~23 files and convert them to Promise.allSettled
    - Any performance impact considerations since we would no longer be fast failing?
    - Probably easier than the above, but it is something the team needs to be aware of.
  - we could convert the extracted tables into jsonb.

- The sort order of consolidated cases after removing one is wrong
- We have remaining east-west issues. Example: 
  - east-west issue with adding/removing a consolidated case
  - uploadCorrespondenceDocumentSequence, which has to do with navigating back to the case details

- generateNoticeOfTrialIssuedInteractor is timing out -- being tackled by John and Nechama

- The data in test is in a corrupt state. Do we need to fix this for Tenille?


- single database to avoid east-west altogether
- fancy routing solution
  - check replication lag and go to east if > x
  - allowing client to specify which endpoint to hit
- provisioned database, with custom setup
- maybe go to east directly in certain scenarios (e.g., getCaseAction)

- just wait
- switch regions (us-west-2) for faster network connectivity
- improved replication speed (practical speedup)

