To Do:

- await decision about edit before serve button generic error, look at/fix the generic error = leave it as is
- await decision about simultaneous document filed across group but chosen to serve to some cases, should it be allowed? 
- await decision to keep/remove bulleted list of docket number and case titles in confirm service modal because the checkboxes already have that information 
- await decision about wording: The following document was (or is?) served on all parties (or cases?). Needs to be accurate when single filed and when multidocketed

- review PR to ensure we add tests to all new lines
- look at Section/My QC Inbox/Outbox and formattedWorkQueue helper and ensure tests are updated
- make sure `state.multiDocketedOriginalCaseDetail` is properly cleared/reset ALWAYS
- remove extra migrations in migrate folder and `multi-docketed-on.ts` script
- update ERD/data-dictionary with new docketEntry fields