To Do:

- [] review PR to ensure we add tests to all new lines
- [] make sure state.multiDocketedOriginalCaseDetail is properly cleared/reset ALWAYS
- [] figure out why filed column is not longer sorting chronologicLLY in section document QC and fix
- [] when we open the service modal, the contactsNeedingPaperService doesn't fetch service preference when a party updates their preference while we are already in the modal. I think this is because we get caseDetail from state and there is no logic to refetch.
- [] bug: when filing a simultaneous doc across the group, ensure its docket No. and index and contents are correct, it seems to be sharing the same index across the group
- [] file across group but serve to some, orphaned bug
- [] `shouldAllowMultiDocketing` function in confirmInitServeModalHelper clean up
- [] check `completeDocketEntryQCInteractor` for TODOs
- [] update ERD with new docketEntry fields
- [] `fileExternalDocumentInteractor` countPagesInDocument: are we using Cody's Map? Don't wanna remove the map and reintroduce a bug that the Map was fixing
- [] look at docket entry Mapper and remove shim after migration
- [] fir filings and proceedings column on the docket record for some cases, it sometimes span the width of the column on some cases in a consolidated group and wrap for others. At an eye glance, it seems that when the text wraps, the filings and proceedings column is smaller.

Migration:

- [] find non-unique docketEntryIds in all docketEntries
- [] for each entry, add the docketNumber of the docketEntries with the same EntryIds to the multiDocketedOn array
- [] stretch goal: if docketEntry on a now-unconsolidated case was found, run it thru our new removeCase to clone s3 doc and reassign documentStorageId 
- [] make lead case of group set to originalFilingCase (maybe) if cant figure out the true original

Migration s3 refactor
- [] 1. Add new documentStorageId column to db
- [] 1. Add shim to docketentry persistence to start writing to documentStorageId column (no data migration yet)
- [] 2. Actually migrate data, populate documentStorageId w/ docketEntryId (make sure db remains accessible during migration)
- [] Switch colors a second time
- [] Celebrate

Migration story work
- [] Add new columns to db, populate those columns immediately (see Migration above)
- [] Danger window - Options: Maintenance mode, try to keep public site running (close down internal site via Cognito), develop more complex system involving feature flags, background migrations etc
- [] Switch colors
- [] Celebrate