To Do:

- [] review PR to ensure we add tests to all new lines
- [] remember not to actually edit the document (on the original filing case) if the original filing case has been unconsolidated
- [x] check if `documentContentsId` also needs to be updated when removing case from consolidated group (can pdf content be edited enough to require new scraped text?) (doesn't matter, duplicate anyway to futureproof)
- [] consider refactoring loadPDFForSigningInteractor aliasing in helpers.ts, directly importing `loadPDFForSigningInteractor` instead of calling from applicationContext
- [x] add documentStorageId to seed data
- [x] make sure `documentIdBeforeSignature` is set to documentStorageId rather than docket EntryId
- [x] `stampDocumentForService` make sure it uses documentStorageId properly
- [x] search for 8477: TODO