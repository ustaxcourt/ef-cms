const React = require('react');

export const PractitionerCaseList = ({ closedCases, openCases }) => {
  return (
    <div id="address-label-cover-sheet">
      TODO: FIX ME HELLO WORLD {closedCases.length} {openCases.length}
    </div>
  );
};
