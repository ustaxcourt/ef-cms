/**
 *
 * @param {object} applicationContext the application context
 * @param {object} providers the providers object
 * @param {string} providers.key the key of the s3 object to check status of
 * @returns {Boolean} true if virus scan complete, false otherwise
 */
exports.getStatusOfVirusScanInteractor = async () =>
  // applicationContext,
  // { key },
  {
    return true;
  };
