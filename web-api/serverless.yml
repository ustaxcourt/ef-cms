service: ef-cms

plugins:
  - serverless-plugin-split-stacks
  - serverless-domain-manager
  - serverless-plugin-bind-deployment-id
  # - serverless-plugin-aws-alerts
  - serverless-offline
  - serverless-prune-plugin
  - serverless-plugin-tracing
  - serverless-latest-layer-version
  - serverless-apigwy-binary

custom:
  prune:
    automatic: true
    number: 3

  serverless-offline:
    host: 0.0.0.0
    port: 3001

  alerts:
    stages:
      - dev
      - stg
      - prod
    dashboards: true
    alarms:
      - functionThrottles
      - functionErrors
      - functionInvocations
      - functionDuration
      - errorExceptions
    definitions:
      errorExceptions:
        description: 'Internal Error Exception Alarm'
        namespace: 'AWS/Lambda'
        metric: Errors
        threshold: 1
        statistic: Sum
        period: 60
        evaluationPeriods: 1
        comparisonOperator: GreaterThanThreshold
        pattern: 'ERROR'
    topics:
      alarm:
        topic: ${self:provider.stage}-cloudwatch-alerts
        notifications:
          - protocol: email
            endpoint: jsutton@flexion.us

  customDomain:
    domainName: efcms-${self:provider.stage}.${opt:domain}
    basePath: 'api'
    endpointType: 'regional'
    certificateName: efcms-${self:provider.stage}.${opt:domain}
    certificateRegion: ${opt:region}
    stage: ${self:provider.stage}
    createRoute53Record: false
    enabled: true
  splitStacks:
    perFunction: true
    perType: true
  stage: ${opt:stage, 'dev'}
  region: ${opt:region, 'us-east-1'}
  vars: ${file(./config/${self:custom.stage}.yml)}

provider:
  name: aws
  tracing: true
  stage: ${self:custom.stage}
  region: ${self:custom.region}
  runtime: nodejs10.x
  memorySize: 768
  timeout: 30
  logRetentionInDays: 7
  s3Endpoint: s3.us-east-1.amazonaws.com
  dynamodbEndpoint: dynamodb.${opt:region}.amazonaws.com
  masterRegion: us-east-1
  userPoolId: us-east-1_7uRkF0Axn
  masterDynamodbEndpoint: dynamodb.us-east-1.amazonaws.com
  clamavDefDir: /opt/var/lib/clamav
  deploymentBucket:
    name: ${env:SLS_DEPLOYMENT_BUCKET}
    serverSideEncryption: AES256
  apiGateway:
    binaryMediaTypes:
      - 'application/pdf'

  environment:
    S3_ENDPOINT: ${self:custom.vars.s3Endpoint, self:provider.s3Endpoint}
    DOCUMENTS_BUCKET_NAME: ${opt:domain}-documents-${opt:stage}-us-east-1
    DYNAMODB_ENDPOINT: ${self:custom.vars.dynamodbEndpoint, self:provider.dynamodbEndpoint}
    MASTER_DYNAMODB_ENDPOINT: ${self:custom.vars.masterDynamodbEndpoint, self:provider.masterDynamodbEndpoint}
    MASTER_REGION: ${self:custom.vars.masterRegion, self:provider.masterRegion}
    STAGE: ${self:custom.stage}
    USER_POOL_ID: ${opt:userPoolId, self:provider.userPoolId}
    NODE_ENV: ${self:custom.vars.nodeEnv}
    GS_LIB: /opt/share/ghostscript/9.06/Resource/Init
    CLAMAV_DEF_DIR: ${self:custom.vars.clamavDefDir, self:provider.clamavDefDir}

  iamRoleStatements:
    - Effect: 'Allow'
      Action:
        - 'xray:PutTraceSegments'
        - 'xray:PutTelemetryRecords'
      Resource:
        - '*'
    - Effect: 'Allow'
      Action:
        - 'lambda:InvokeFunction'
      Resource:
        - arn:aws:lambda:${opt:region}:${opt:accountId}:function:${self:service}-${opt:stage, self:provider.stage}-*
    - Effect: 'Allow'
      Action:
        - cognito-idp:AdminCreateUser
        - cognito-idp:AdminGetUser
      Resource:
        - arn:aws:cognito-idp:${opt:region}:${opt:accountId}:userpool/${self:provider.environment.USER_POOL_ID}
    - Effect: 'Allow'
      Action:
        - 's3:*'
      Resource:
        Fn::Join:
          - ''
          - - 'arn:aws:s3:::'
            - ${self:provider.environment.DOCUMENTS_BUCKET_NAME}
            - '/*'
    - Effect: 'Allow'
      Action:
        - dynamodb:Query
        - dynamodb:Scan
        - dynamodb:GetItem
        - dynamodb:PutItem
        - dynamodb:BatchGetItem
        - dynamodb:UpdateItem
        - dynamodb:DeleteItem
        - dynamodb:BatchWriteItem
      Resource:
        - arn:aws:dynamodb:${opt:region}:${opt:accountId}:table/${opt:efcmsTableName}
        - arn:aws:dynamodb:${opt:region}:${opt:accountId}:table/${opt:efcmsTableName}/index/*
        - arn:aws:dynamodb:${self:provider.environment.MASTER_REGION}:${opt:accountId}:table/${opt:efcmsTableName}
        - arn:aws:dynamodb:${self:provider.environment.MASTER_REGION}:${opt:accountId}:table/${opt:efcmsTableName}/index/*

package:
  exclude:
    - storage/**
    - coverage/**
    - terraform/**
    - cognito/**
    - node_modules/**
    - .cache/**
    - .dynamodb/**
    - .nyc_output/**
    - dist/**
    - runtimes/**

  excludeDevDependencies: true

resources:
  Resources:
    #####
    # This Authorizer is used for validating the JWT token before invoking the lambda
    #####
    ApiGatewayAuthorizer:
      Type: AWS::ApiGateway::Authorizer
      Properties:
        Name: CognitoUserPool
        Type: COGNITO_USER_POOLS
        IdentitySource: method.request.header.Authorization
        RestApiId:
          Ref: ApiGatewayRestApi
        ProviderARNs:
          - arn:aws:cognito-idp:${opt:region}:${opt:accountId}:userpool/${self:provider.environment.USER_POOL_ID}

    #####
    # Begin Stage for API Gateway Logging
    #####
    ApiGatewayStage:
      Type: AWS::ApiGateway::Stage
      Properties:
        DeploymentId:
          Ref: __deployment__
        RestApiId:
          Ref: ApiGatewayRestApi
        StageName: ${opt:stage}
        MethodSettings:
          - DataTraceEnabled: true
            HttpMethod: '*'
            LoggingLevel: INFO
            ResourcePath: '/*'
            MetricsEnabled: true

functions:
  getUploadPolicy:
    handler: src/apiHandlers.getUploadPolicyLambda
    events:
      - http:
          path: documents/upload-policy
          method: get
          cors: true
          authorizer:
            type: COGNITO_USER_POOLS
            authorizerId:
              Ref: ApiGatewayAuthorizer

  getDocumentDownloadUrl:
    handler: src/apiHandlers.getDocumentDownloadUrlLambda
    events:
      - http:
          path: documents/{documentId}/document-download-url
          method: get
          cors: true

  setWorkItemAsRead:
    handler: src/apiHandlers.setWorkItemAsReadLambda
    events:
      - http:
          path: work-items/{workItemId}/read
          method: post
          cors: true

  getDownloadPolicyUrl:
    handler: src/apiHandlers.downloadPolicyUrlLambda
    events:
      - http:
          path: documents/{documentId}/download-policy-url
          method: get
          cors: true
          authorizer:
            type: COGNITO_USER_POOLS
            authorizerId:
              Ref: ApiGatewayAuthorizer

  sanitizePdf:
    handler: src/apiHandlers.sanitizePdfLambda
    layers:
      - arn:aws:lambda:${opt:region}:${opt:accountId}:layer:${opt:stage}-ghostscript:latest
    memorySize: 3008
    timeout: 900
    events:
      - http:
          path: documents/{documentId}/sanitize
          method: post
          cors: true
          # async: true
          authorizer:
            type: COGNITO_USER_POOLS
            authorizerId:
              Ref: ApiGatewayAuthorizer

  runBatchProcess:
    handler: src/apiHandlers.runBatchProcessLambda
    events:
      - http:
          path: runBatchProcess
          method: post
          cors: true
          authorizer:
            type: COGNITO_USER_POOLS
            authorizerId:
              Ref: ApiGatewayAuthorizer

  getCasesByUser:
    handler: src/apiHandlers.getCasesByUserLambda
    events:
      - http:
          path: users/{userId}/cases
          method: get
          cors: true
          authorizer:
            type: COGNITO_USER_POOLS
            authorizerId:
              Ref: ApiGatewayAuthorizer

  verifyCaseForUser:
    handler: src/apiHandlers.verifyCaseForUserLambda
    events:
      - http:
          path: users/{userId}/case/{caseId}
          method: get
          cors: true
          authorizer:
            type: COGNITO_USER_POOLS
            authorizerId:
              Ref: ApiGatewayAuthorizer

  verifyPendingCaseForUser:
    handler: src/apiHandlers.verifyPendingCaseForUserLambda
    events:
      - http:
          path: users/{userId}/case/{caseId}/pending
          method: get
          cors: true
          authorizer:
            type: COGNITO_USER_POOLS
            authorizerId:
              Ref: ApiGatewayAuthorizer

  practitionerCaseAssociation:
    handler: src/apiHandlers.practitionerCaseAssociationLambda
    events:
      - http:
          path: users/{userId}/case/{caseId}
          method: put
          cors: true
          authorizer:
            type: COGNITO_USER_POOLS
            authorizerId:
              Ref: ApiGatewayAuthorizer

  practitionerPendingCaseAssociation:
    handler: src/apiHandlers.practitionerPendingCaseAssociationLambda
    events:
      - http:
          path: users/{userId}/case/{caseId}/pending
          method: put
          cors: true
          authorizer:
            type: COGNITO_USER_POOLS
            authorizerId:
              Ref: ApiGatewayAuthorizer

  assignWorkItems:
    handler: src/apiHandlers.assignWorkItemsLambda
    events:
      - http:
          path: work-items
          method: put
          cors: true
          authorizer:
            type: COGNITO_USER_POOLS
            authorizerId:
              Ref: ApiGatewayAuthorizer

  getWorkItem:
    handler: src/apiHandlers.getWorkItemLambda
    events:
      - http:
          path: work-items/{workItemId}
          method: get
          cors: true
          authorizer:
            type: COGNITO_USER_POOLS
            authorizerId:
              Ref: ApiGatewayAuthorizer

  forwardWorkItem:
    handler: src/apiHandlers.forwardWorkItemLambda
    events:
      - http:
          path: work-items/{workItemId}/assignee
          method: put
          cors: true
          authorizer:
            type: COGNITO_USER_POOLS
            authorizerId:
              Ref: ApiGatewayAuthorizer

  completeWorkItem:
    handler: src/apiHandlers.completeWorkItemLambda
    events:
      - http:
          path: work-items/{workItemId}/complete
          method: put
          cors: true
          authorizer:
            type: COGNITO_USER_POOLS
            authorizerId:
              Ref: ApiGatewayAuthorizer

  getInboxMessagesForUser:
    handler: src/apiHandlers.getInboxMessagesForUserLambda
    events:
      - http:
          path: users/{userId}/messages/inbox
          method: get
          cors: true
          authorizer:
            type: COGNITO_USER_POOLS
            authorizerId:
              Ref: ApiGatewayAuthorizer

  getSentMessagesForUser:
    handler: src/apiHandlers.getSentMessagesForUserLambda
    events:
      - http:
          path: users/{userId}/messages/sent
          method: get
          cors: true
          authorizer:
            type: COGNITO_USER_POOLS
            authorizerId:
              Ref: ApiGatewayAuthorizer

  getInboxMessagesForSection:
    handler: src/apiHandlers.getInboxMessagesForSectionLambda
    events:
      - http:
          path: sections/{section}/messages/inbox
          method: get
          cors: true
          authorizer:
            type: COGNITO_USER_POOLS
            authorizerId:
              Ref: ApiGatewayAuthorizer

  getSentMessagesForSection:
    handler: src/apiHandlers.getSentMessagesForSectionLambda
    events:
      - http:
          path: sections/{section}/messages/sent
          method: get
          cors: true
          authorizer:
            type: COGNITO_USER_POOLS
            authorizerId:
              Ref: ApiGatewayAuthorizer

  getDocumentQCInboxForUser:
    handler: src/apiHandlers.getDocumentQCInboxForUserLambda
    events:
      - http:
          path: users/{userId}/document-qc/inbox
          method: get
          cors: true
          authorizer:
            type: COGNITO_USER_POOLS
            authorizerId:
              Ref: ApiGatewayAuthorizer

  getDocumentQCBatchedForUser:
    handler: src/apiHandlers.getDocumentQCBatchedForUserLambda
    events:
      - http:
          path: users/{userId}/document-qc/batched
          method: get
          cors: true
          authorizer:
            type: COGNITO_USER_POOLS
            authorizerId:
              Ref: ApiGatewayAuthorizer

  getDocumentQCServedForUser:
    handler: src/apiHandlers.getDocumentQCServedForUserLambda
    events:
      - http:
          path: users/{userId}/document-qc/served
          method: get
          cors: true
          authorizer:
            type: COGNITO_USER_POOLS
            authorizerId:
              Ref: ApiGatewayAuthorizer

  getDocumentQCInboxForSection:
    handler: src/apiHandlers.getDocumentQCInboxForSectionLambda
    events:
      - http:
          path: sections/{section}/document-qc/inbox
          method: get
          cors: true
          authorizer:
            type: COGNITO_USER_POOLS
            authorizerId:
              Ref: ApiGatewayAuthorizer

  getDocumentQCBatchedForSection:
    handler: src/apiHandlers.getDocumentQCBatchedForSectionLambda
    events:
      - http:
          path: sections/{section}/document-qc/batched
          method: get
          cors: true
          authorizer:
            type: COGNITO_USER_POOLS
            authorizerId:
              Ref: ApiGatewayAuthorizer

  getDocumentQCServedForSection:
    handler: src/apiHandlers.getDocumentQCServedForSectionLambda
    events:
      - http:
          path: sections/{section}/document-qc/served
          method: get
          cors: true
          authorizer:
            type: COGNITO_USER_POOLS
            authorizerId:
              Ref: ApiGatewayAuthorizer

  getNotifications:
    handler: src/apiHandlers.getNotificationsLambda
    events:
      - http:
          path: notifications
          method: get
          cors: true
          authorizer:
            type: COGNITO_USER_POOLS
            authorizerId:
              Ref: ApiGatewayAuthorizer

  checkForReadyForTrialCases:
    handler: src/apiHandlers.checkForReadyForTrialCases
    events:
      - schedule: cron(0 7 * * ? *)

  swagger:
    handler: src/apiHandlers.swaggerLambda
    events:
      - http:
          path: swagger
          method: get
          cors: true

  swaggerJson:
    handler: src/apiHandlers.swaggerJsonLambda
    events:
      - http:
          path: swagger.json
          method: get
          cors: true

  virusScan:
    handler: src/apiHandlers.virusScanPdfLambda
    memorySize: 3008
    timeout: 900
    layers:
      - arn:aws:lambda:${opt:region}:${opt:accountId}:layer:${opt:stage}-clamav:latest
    events:
      - http:
          path: documents/{documentId}/virus-scan
          method: post
          cors: true
          async: true

  validate:
    handler: src/apiHandlers.validatePdfLambda
    events:
      - http:
          path: documents/{documentId}/validate
          method: post
          cors: true
    alarms:
      - errorExceptions

  getTrialSessions:
    handler: src/apiHandlers.getTrialSessionsLambda
    events:
      - http:
          path: trial-sessions
          method: get
          cors: true
          authorizer:
            type: COGNITO_USER_POOLS
            authorizerId:
              Ref: ApiGatewayAuthorizer

  createTrialSession:
    handler: src/apiHandlers.createTrialSessionLambda
    events:
      - http:
          path: trial-sessions
          method: post
          cors: true
          authorizer:
            type: COGNITO_USER_POOLS
            authorizerId:
              Ref: ApiGatewayAuthorizer

  getTrialSessionDetails:
    handler: src/apiHandlers.getTrialSessionDetailsLambda
    events:
      - http:
          path: trial-sessions/{trialSessionId}
          method: get
          cors: true
          authorizer:
            type: COGNITO_USER_POOLS
            authorizerId:
              Ref: ApiGatewayAuthorizer

  setTrialSessionAsSwingSession:
    handler: src/apiHandlers.setTrialSessionAsSwingSessionLambda
    events:
      - http:
          path: trial-sessions/{trialSessionId}/set-swing-session
          method: post
          cors: true
          authorizer:
            type: COGNITO_USER_POOLS
            authorizerId:
              Ref: ApiGatewayAuthorizer

  getEligibleCasesForTrialSession:
    handler: src/apiHandlers.getEligibleCasesForTrialSessionLambda
    events:
      - http:
          path: trial-sessions/{trialSessionId}/eligible-cases
          method: get
          cors: true
          authorizer:
            type: COGNITO_USER_POOLS
            authorizerId:
              Ref: ApiGatewayAuthorizer

  setTrialSessionCalendar:
    handler: src/apiHandlers.setTrialSessionCalendarLambda
    events:
      - http:
          path: trial-sessions/{trialSessionId}/set-calendar
          method: post
          cors: true
          authorizer:
            type: COGNITO_USER_POOLS
            authorizerId:
              Ref: ApiGatewayAuthorizer

  getCalendaredCasesForTrialSession:
    handler: src/apiHandlers.getCalendaredCasesForTrialSessionLambda
    events:
      - http:
          path: trial-sessions/{trialSessionId}/get-calendared-cases
          method: get
          cors: true
          authorizer:
            type: COGNITO_USER_POOLS
            authorizerId:
              Ref: ApiGatewayAuthorizer

  createCourtIssuedOrderPdfFromHtml:
    runtime: nodejs8.10
    handler: src/apiHandlers.createCourtIssuedOrderPdfFromHtmlLambda
    layers:
      - arn:aws:lambda:${opt:region}:${opt:accountId}:layer:${opt:stage}-puppeteer:latest
    memorySize: 3008
    timeout: 900
    events:
      - http:
          path: court-issued-order
          method: post
          cors: true
          contentHandling: CONVERT_TO_BINARY
          authorizer:
            type: COGNITO_USER_POOLS
            authorizerId:
              Ref: ApiGatewayAuthorizer

  getUsersInSection:
    handler: src/apiHandlers.getUsersInSectionLambda
    events:
      - http:
          path: sections/{section}/users
          method: get
          cors: true
          authorizer:
            type: COGNITO_USER_POOLS
            authorizerId:
              Ref: ApiGatewayAuthorizer
