#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

npx lint-staged
./check-auto-approve.sh

find . \
    -iname "*.sh" \
    -not -path "./node_modules/*" \
    -not -path "./.husky/**/*.sh" \
    -exec shellcheck -x -S style "{}" \;