## Things to do
- refactor getCaseMetadataWIthCounsel => getCaseMetadatasWIthCounsel to speed up consolidate cases


# Things we know
- The puppeteer layer does not seem to cause this issue. A lambda deployed on local computer using identical lambda layer version as a circle lambda will have 0 errors
- The machine building the lambda seems to be involved in producing this bug. Identical lambdas can be deployed using circle and Mac, and only the circle build will have problems
- This problem does not seem to occur in staging. 2000 invocations of opening chromium 30 times each (60000 iterations) could not produce the error.
- It happens on warm lambdas, cold lambdas, on the 10th iteration and on the 1st iteration of opening the browser. Seemingly random?