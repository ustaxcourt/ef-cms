## Things to do
- refactor getCaseMetadataWIthCounsel => getCaseMetadatasWIthCounsel to speed up consolidate cases
- isValidConnection(), how to deal with expired connections
  - store last time we did isValidConnection and only call when it has been "long enough"
  - understand the timeout mechanism so well that we don't even have to handle expirations
  - try catch, and then do isValidConnection and reconnect if not
  - some timer based mechanism to keep the connection "fresh enough"
  - look at error code and error messages to see if connection is invalid (to avoid querying db)
    - if we can be specific, then maybe; if not, then nope
  For sure no:
  - use secret, undocumented Postgres field to see if connection is valid