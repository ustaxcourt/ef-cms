## Things to do
- do not fetch consolidated cases in most situations
  - need it for:
    - getDownloadPolicyUrlInteractor (because it calls userHasAccessToCase)
    - getPublicDownloadPolicyUrlInteractor (because it calls DocketEntry.isDownloadable, which calls userHasAccessToCase)
    - anything that calls formatDocketEntryOnDocketRecord
    - anything that calls getFormattedDocketEntry
    - anything that calls isUserPartOfGroup
    - 
- only use getCasesBydocketNumbers
- only use upsertCases
- isValidConnection(), how to deal with expired connections

