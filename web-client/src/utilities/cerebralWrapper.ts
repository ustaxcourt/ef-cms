import { Get, parallel, props, sequences } from 'cerebral';
import App from 'cerebral';

// eslint-disable-next-line import/no-default-export
export default App;
export { parallel, props, sequences, Get };
