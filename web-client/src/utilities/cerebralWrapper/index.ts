import { Get, parallel, props, sequences, state } from 'cerebral';
import App from 'cerebral';

// eslint-disable-next-line import/no-default-export
export default App;
export { parallel, props, sequences, state, Get };
