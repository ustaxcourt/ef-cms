/**
 * get next step prop as set to the first step
 *
 * @returns {object} the prop of the alert success message
 */
export const getInitialNextStepAction = () => {
  return {
    nextStep: 1,
  };
};
